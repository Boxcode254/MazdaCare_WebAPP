# Vite / Supabase (required for the app)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional
VITE_GOOGLE_MAPS_API_KEY=

# Optional: push to GitHub when HTTPS has no saved credentials (never commit real values)
# Create a new PAT at https://github.com/settings/tokens — revoke any token that was exposed.
# GITHUB_TOKEN=
